1. Is the `APPROVED_CATEGORIES` list future-proof? If the Flutter team decides to add a new category in the next few
   months, it would mean I would have to come back and edit the code. Is this something we want or is there a more
   efficient way to handle this? For instance, depending on the `catalog_extractor` to scrape the Flutter website (using
   scrape since there is no place in the Flutter docs where the widgets are categorized as such) to retrieve the
   categories?

